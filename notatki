BAZA:
sudo systemctl start mysql #uruchamia serwer mysql
sudo /usr/bin/mysql -u root -p #uruchamia shell mysql
#TWORZENIE BAZY:
CREATE DATABASE data_thesis;


BACKEND:
pipenv shell #uruchamia wirtualne srodowisko
pipenv install <nazwa_paczki> # instaluje brakujace pakiety do naszego srodowiska

